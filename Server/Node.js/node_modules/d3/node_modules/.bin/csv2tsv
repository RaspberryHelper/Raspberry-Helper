#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../d3-dsv/bin/csv2tsv" "$@"
  ret=$?
else 
  node  "$basedir/../d3-dsv/bin/csv2tsv" "$@"
  ret=$?
fi
exit $ret
